function names(array){
    const names = ["Megan", "Agnes", "Shalom", "Mwanasha"]

    console.log(names.reverse().sort());
    return {return:names};
}
let numbers=[23,54,38,98,10];
let newNumbers=[];
numbers.forEach(function(newNumbers)){
    newNumbers.push(newNumbers*2)
}
